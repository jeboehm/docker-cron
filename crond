#!/bin/sh

docker rm -f crond
docker run -d \
  --name crond \
  -v /var/run/docker.sock:/var/run/docker.sock:ro \
  -v crond-data:/var/spool/cron/crontabs \
  -m 32M \
  --restart=always \
  jeboehm/cron:latest
